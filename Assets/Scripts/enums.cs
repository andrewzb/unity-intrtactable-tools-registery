
public enum ToolGrabType {
    grabed,
    grabedLeft,
    grabedRight,
    notGrabed,
    any
}

public enum ToolTrakingType {
    single,
    multiple
}

/*
public enum ToolSelectionType {
    inRange,
    iclosest,
}
*/

public enum TypeInteractable {
    slicer,
    poker,
}
